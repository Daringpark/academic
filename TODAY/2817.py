

def dfs(n, s): # 재귀식으로 1, 0
    
    # 기저 조건
    
    
    # 끝까지 도달했을 때
    if n == N :
        return








# 최소 1이상의 N개의 수를 선택하여 그 합이 원하는 K값을 만드는 경우의 수를 구하세요
# 2 <= N <= 20, 1 <= K <= 1000
# 원소 값은 1이상 100 이하

T = int(input())
for tc in range(1, T+1):
    
    N, K = map(int, input().split())
    numbers = list(map(int, input().split()))
    
    
    
    
    dfs(0, 0)
    
    
    
    
    
    print(f'#{tc}')